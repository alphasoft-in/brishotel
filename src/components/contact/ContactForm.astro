<section id="contactanos" class="py-10 md:py-16">
    <div class="max-w-7xl mx-auto px-6">
        <h2
            class="text-3xl sm:text-4xl md:text-5xl font-kestrel font-bold text-center text-[#5a3d37] mb-4 md:mb-6"
        >
            Contáctanos
        </h2>
        <p
            class="text-xs sm:text-sm md:text-base merriweather text-center text-[#5a3d37] mb-6 md:mb-10 max-w-2xl mx-auto opacity-80"
        >
            ¿Tienes alguna pregunta o deseas reservar? <br
                class="hidden sm:block"
            /> Envíanos un mensaje y nuestro equipo se pondrá en contacto contigo
            lo antes posible.
        </p>

        <div
            class="bg-white merriweather p-6 md:p-10 rounded-3xl shadow-[0_20px_50px_rgba(0,0,0,0.1)] mx-auto max-w-2xl border border-gray-50"
        >
            <form onsubmit="sendToWhatsApp(event)" class="space-y-6">
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <div class="flex flex-col">
                        <label
                            for="name"
                            class="text-[#5a3d37] text-sm font-semibold mb-2"
                            >Nombre Completo</label
                        >
                        <input
                            type="text"
                            id="name"
                            placeholder="Tu nombre"
                            class="border border-[#759120] rounded-xl px-4 py-3 text-[#5a3d37] focus:outline-none focus:ring-2 focus:ring-[#759120] transition-all"
                            required
                        />
                    </div>

                    <div class="flex flex-col">
                        <label
                            for="email"
                            class="text-[#5a3d37] text-sm font-semibold mb-2"
                            >Correo Electrónico</label
                        >
                        <input
                            type="email"
                            id="email"
                            placeholder="Tu correo electrónico"
                            class="border border-[#759120] rounded-xl px-4 py-3 text-[#5a3d37] focus:outline-none focus:ring-2 focus:ring-[#759120] transition-all"
                            required
                        />
                    </div>
                </div>

                <div class="flex flex-col">
                    <label
                        for="message"
                        class="text-[#5a3d37] text-sm font-semibold mb-2"
                        >Tu Mensaje</label
                    >
                    <textarea
                        id="message"
                        placeholder="Escribe tu mensaje aquí"
                        class="border border-[#759120] rounded-xl px-4 py-3 text-[#5a3d37] focus:outline-none focus:ring-2 focus:ring-[#759120] resize-none h-32 transition-all"
                        required></textarea>
                </div>

                <div class="flex justify-center mt-8">
                    <button
                        type="submit"
                        class="merriweather bg-[#759120] text-white px-8 py-3.5 text-[10px] sm:text-xs font-bold tracking-[0.15em] uppercase hover:bg-[#5a3d37] transition rounded shadow-sm hover:shadow-md active:scale-95 cursor-pointer"
                    >
                        Enviar Mensaje
                    </button>
                </div>
            </form>
        </div>
    </div>
</section>

<script is:inline>
    function sendToWhatsApp(event) {
        event.preventDefault();
        const phone = "51940948536"; // tu número de WhatsApp
        const name = document.getElementById("name").value;
        const email = document.getElementById("email").value;
        const message = document.getElementById("message").value;

        const text = `Hola, soy ${name}.%0ACorreo: ${email}%0AMensaje: ${message}`;
        const url = `https://wa.me/${phone}?text=${text}`;
        window.open(url, "_blank");
    }
</script>
